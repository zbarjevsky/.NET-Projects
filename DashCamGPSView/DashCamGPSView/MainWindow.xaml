<Window x:Class="DashCamGPSView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DashCamGPSView"
        xmlns:controls="clr-namespace:DashCamGPSView.Controls"
        mc:Ignorable="d"
        Loaded="Window_Loaded" Closed="Window_Closed"
        WindowStartupLocation="Manual"
        Left="10" Top="10"
        Title="DashCam GPS Viewer" Height="750" Width="1600">
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Open" CanExecute="Open_CanExecute" Executed="Open_Executed" />
        <CommandBinding Command="MediaCommands.Play" CanExecute="Play_CanExecute" Executed="Play_Executed" />
        <CommandBinding Command="MediaCommands.Pause" CanExecute="Pause_CanExecute" Executed="Pause_Executed" />
        <CommandBinding Command="MediaCommands.Stop" CanExecute="Stop_CanExecute" Executed="Stop_Executed" />
    </Window.CommandBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="500"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Border Grid.Column="0" Grid.Row="1" BorderThickness="2" BorderBrush="Brown" Background="Beige">
            <controls:FilesTreeUserControl x:Name="treeGroups"/>
        </Border>
        <GridSplitter Grid.Column="1" Grid.Row="1" Grid.RowSpan="1" HorizontalAlignment="Stretch"/>
        <Border Grid.Column="2" Grid.Row="1" BorderThickness="2" BorderBrush="YellowGreen" Background="Black">
            <local:VideoPlayer x:Name="playerR"/>
        </Border>
        <GridSplitter Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="2" 
                      HorizontalAlignment="Stretch"
                      DragCompleted="GridSplitter1_DragCompleted"/>
        <Border Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="3" BorderThickness="2" BorderBrush="YellowGreen" Background="Black">
            <local:VideoPlayer x:Name="playerF"/>
        </Border>
        <GridSplitter Grid.Column="3" Grid.Row="1" Grid.RowSpan="3" HorizontalAlignment="Stretch"/>
        <Border Grid.Column="4" Grid.ColumnSpan="3" Grid.Row="1" Grid.RowSpan="3" BorderThickness="2" BorderBrush="YellowGreen">
            <controls:Map x:Name="MainMap" MaxZoom="24" MinZoom="1" />
        </Border>
        <Border Grid.Column="4" Grid.Row="4" Grid.RowSpan="3" BorderThickness="2" BorderBrush="Goldenrod">
            <controls:GpsInfoUserControl x:Name="gpsInfo"/>
        </Border>
        <ToolBar Grid.Column="0" Grid.Row="4" Grid.ColumnSpan="3" >
            <Button Command="ApplicationCommands.Open" ToolTip="Open">
                <local:AutoGreyableImage Source="/DashCamGPSView;component/Images/map_32px.png" />
            </Button>
            <Separator />
            <Button Command="MediaCommands.Play" ToolTip="Play" >
                <local:AutoGreyableImage  Source="/Images/PlayE.png" />
            </Button>
            <Button Command="MediaCommands.Pause" ToolTip="Pause" >
                <local:AutoGreyableImage Source="/Images/PauseE.png" />
            </Button>
            <Button Command="MediaCommands.Stop" ToolTip="Stop" >
                <local:AutoGreyableImage Source="/Images/StopE.png" />
            </Button>
            <TextBlock Name="txtFileName"  Text="..." VerticalAlignment="Center" Margin="10,0" FontSize="16"/>
        </ToolBar>
        
        <TextBlock Name="txtGPSInfo" Grid.Row="5" Grid.ColumnSpan="3"  
                   Text="GPS Info" 
                   VerticalAlignment="Center" Margin="10,0" FontSize="22"/>

        <StatusBar Grid.Row="6" Grid.ColumnSpan="3">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem>
                <TextBlock Name="lblProgressStatus">00:00:00</TextBlock>
            </StatusBarItem>
            <StatusBarItem Grid.Column="1" HorizontalContentAlignment="Stretch">
                <Slider Name="sliProgress" 
                        SmallChange="1" LargeChange="10"
                        Thumb.DragStarted="sliProgress_DragStarted"  
                        Thumb.DragCompleted="sliProgress_DragCompleted" 
                        ValueChanged="sliProgress_ValueChanged" />
            </StatusBarItem>
            <StatusBarItem Grid.Column="2">
                <ProgressBar Name="pbVolume" Width="50" Height="12" Maximum="1" Value="{Binding ElementName=mePlayer, Path=Volume}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
