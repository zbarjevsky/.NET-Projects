<Window x:Class="MultiPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MultiPlayer"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        Background="SlateGray"
        mc:Ignorable="d"
        Icon="Yin-Yang.ico"
        Title="Multi Media Player - 6x" Height="950" Width="1900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="35"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Margin="1" Background="Gray" HorizontalAlignment="Center" VerticalAlignment="Top">
            <Grid.LayoutTransform>
                <RotateTransform Angle="90" />
            </Grid.LayoutTransform>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Open..." ToolTip="Open..."
                    Click="Open_Click"
                    Margin="3" Padding="5,3"/>
            <Button Grid.Column="1" Content="Save As..." ToolTip="Save As..."
                    Click="SaveAs_Click"
                    Margin="3" Padding="5,3" />
            <Button Grid.Column="2" Content="Close All" ToolTip="Close All"
                    Click="CloseAll_Click"
                    Margin="3" Padding="5,3" />
            <Button Grid.Column="3" Content="Exit" ToolTip="Exit"
                    Click="Exit_Click"
                    Margin="3" Padding="5,3" />
        </Grid>
        <Grid Grid.Column="1" Background="Gray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="50"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*" MinWidth="50"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*" MinWidth="50"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="5"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <GridSplitter Grid.Column="1" Grid.RowSpan="3" Background="Gray" Width="5" ResizeDirection="Columns" ResizeBehavior="PreviousAndNext" HorizontalAlignment="Left" VerticalAlignment="Stretch"/>
            <GridSplitter Grid.Column="3" Grid.RowSpan="3" Background="Gray" Width="5" ResizeDirection="Columns" ResizeBehavior="PreviousAndNext" HorizontalAlignment="Left" VerticalAlignment="Stretch"/>

            <GridSplitter Grid.ColumnSpan="5" Grid.Row="1" Background="LightGray" Height="5" HorizontalAlignment="Stretch" />

            <local:VideoPlayerUserControl x:Name="_video00" Grid.Column="0" Grid.Row="0" Background="AliceBlue" />
            <local:VideoPlayerUserControl x:Name="_video01" Grid.Column="2" Grid.Row="0" Background="Pink" />
            <local:VideoPlayerUserControl x:Name="_video02" Grid.Column="4" Grid.Row="0" Background="Gold" />
            <local:VideoPlayerUserControl x:Name="_video10" Grid.Column="0" Grid.Row="2" Background="LightYellow" />
            <local:VideoPlayerUserControl x:Name="_video11" Grid.Column="2" Grid.Row="2" Background="LightGreen" />
            <local:VideoPlayerUserControl x:Name="_video12" Grid.Column="4" Grid.Row="2" Background="LightGray" />
        </Grid>
    </Grid>
</Window>
