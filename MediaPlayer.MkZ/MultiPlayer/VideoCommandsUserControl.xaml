<UserControl x:Class="MultiPlayer.VideoCommandsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MultiPlayer"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="800">
    <Grid Margin="0">
        <Grid Grid.Row="1" Background="AliceBlue" Margin="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="open" Click="Open_Click" />
            <Button Grid.Column="1" Content="play/pause" Click="PlayPause_Click" />
            <Button Grid.Column="2" Content="next" />
            <Button Grid.Column="3" Content="prev" />
            <ComboBox x:Name="_speed" Grid.Column="4" SelectedIndex="3" SelectionChanged="Speed_Selected" >
                <ComboBoxItem Content=".12x" />
                <ComboBoxItem Content=".25x" />
                <ComboBoxItem Content=".5x" />
                <ComboBoxItem Content="1x" />
                <ComboBoxItem Content="1.5x" />
            </ComboBox>
            <ComboBox x:Name="_fit" Grid.Column="5" SelectedIndex="2" SelectionChanged="Fit_Selected" >
                <ComboBoxItem Content="FitWindow" />
                <ComboBoxItem Content="FitWidth" />
                <ComboBoxItem Content="FitHeight" />
                <ComboBoxItem Content="Original" />
            </ComboBox>
            <Slider x:Name="_position" Grid.Column="6" Margin="2" 
                    Value="33" Maximum="300" Minimum="0" IsMoveToPointEnabled="True" 
                    Thumb.DragStarted="Pos_DragStarted"
                    Thumb.DragCompleted="Pos_DragCompleted" />
            <Slider x:Name="_volume" Grid.Column="7" Margin="2" Value="150" Maximum="1000" Minimum="0" Width="60" ValueChanged="Volume_ValueChanged"/>
        </Grid>
    </Grid>
</UserControl>
