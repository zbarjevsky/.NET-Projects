<Window x:Class="DesktopManagerUX.Controls.PopupInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopManagerUX.Controls" xmlns:wpf="clr-namespace:MZ.WPF"
        mc:Ignorable="d"
        WindowStartupLocation="Manual"
        WindowStyle="None"
        ResizeMode="NoResize"
        Title="PopupInfoWindow" Height="250" Width="120">
    <Grid Background="Navy">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <wpf:GradientProgressBar Margin="10" Value="{Binding Value, ElementName=_slider}">
            <wpf:GradientProgressBar.LayoutTransform>
                <RotateTransform CenterX="0.5" CenterY="0.5" Angle="-90"/>
            </wpf:GradientProgressBar.LayoutTransform>
        </wpf:GradientProgressBar>
        <Slider x:Name="_slider" Grid.Column="1" Margin="10"
                Orientation="Vertical" HorizontalAlignment="Center"
                Minimum="0" Maximum="180" Value="30">
            
        </Slider>
    </Grid>
</Window>
